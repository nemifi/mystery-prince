# 全面移行の完了記録

実施日: 2026-09-08。移行前の基準: `c9984b1`。旧形式・旧セーブ・旧APIの互換層は設けていない。

## 移行した範囲

| 対象 | 移行後 |
| --- | --- |
| 共通基盤 | 公開版検証、実装解決、能力の許可、状態、リビジョン、操作結果、外部結果の記録 |
| 体験の意味 | 固定脚本と生成対話型の2領域・2形式。別々の状態と完了条件 |
| 制作の正本 | 作品意図、役、配役、ルール、台詞・証拠文、資産を分離 |
| 既存作品 | 22場面と23場面の全編を移行。各人物のA/B立ち絵を維持 |
| 配役 | 台詞・証拠文の名前を役の参照へ置換し、公開時に束縛 |
| 公開 | 5公開版、9資産リビジョン、1研究定義を生成。内容と実装のハッシュを固定 |
| 実行 | JavaScriptと独立Pythonの固定脚本エンジン、生成対話型エンジン |
| 表示 | Webとターミナル。観測データだけを受け取る |
| 保存 | Memory、SQLite、IndexedDB。同じ契約で保存・CAS・外部結果を扱う |
| 復元 | 二重操作、同時再送、障害後再開、終了後に届く外部結果、端末間のセーブ転送 |
| 生成 | 異なる2実装。生成結果を記録し、再開・保存先移行で再抽選しない |
| 研究 | 明示的な編集、AB/BA、参加者scope、段階的開示、JSON出力、厳格な分析 |
| 運用 | 新しい開発・検証・凍結・分析コマンド、公開ワークフロー、参加者URL |

## 撤去

`prototype/`、`schemas/` の旧共通契約、`examples/` の旧実行形式、旧Pythonツール、旧ブラウザースモーク、旧Python依存ファイルを撤去した。Storage/fetchの差し替え、DOM監視による本文加工、CSSに埋めた役の割当ては現行実装にない。

旧設計・脚本・研究文書は [archive/pre-platform](archive/pre-platform/README.md) へ移し、失効した過去の記録であることを明示した。GitHub Pagesの配信元は `dist/site/` に変更した。

実表示で判明した旧画像の問題も修正した。復号できない列車背景と汎用立ち絵2点を除き、画像内に旧UIの文字が入っていたホテル背景も交換した。新背景は外部資産へ依存しないSVG。6点のA/B立ち絵は元の役別画像を維持した。

## 実行した確認

- `npm run verify`: **31 / 31成功**。5公開版・9資産リビジョン・研究revisionを検証。
- 別一時ディレクトリーへのビルドで、公開カタログ、実装ロック、Webバンドルが一致。
- 固定脚本4公開版の全編をJavaScriptと独立Pythonで実行し、誤答を含む状態・観測・イベントが一致。
- 同じ保存契約をMemory、SQLite、実ブラウザーのIndexedDBで検証。
- 生成器2実装をそれぞれ12種のseedで検証。真相の変化、仮説の撤回、両方の不可逆な結末を確認。
- `npm run test:browser`: **成功**。ABは1280px、BAは390pxで2本を完了し、途中リロード、回答保存、停止位置の復元、開示、出力を確認。
- Web→SQLiteのターミナル→別ブラウザーへ実セーブを渡し、同じセッションの続きから完了。
- 対話型の質問、疑いの変更、最終結論、最終結論後のリロードを確認。
- 全公開画像をブラウザーでdecodeし、PC/スマートフォンのスクリーンショットを目視確認。横方向のはみ出し、画像読み込み失敗、JavaScriptエラーなし。
- 合成参加者2件のエクスポートを分析CLIへ渡し、完了数2・期待する平均4を確認。
- 現行文書のローカルリンクと差分の空白エラーを確認。

検証画像・合成データは `build/qa/` に出力し、Gitには含めない。正確な研究IDは [ops/wave1-release.json](../ops/wave1-release.json) に保存した。

## 確認範囲の区別

リポジトリー内の実装・制作入力・配信定義の移行は完了。GitHub Actionsの完了待ちや公開サイトの反映確認は実施していない。

実参加者による作品の面白さ、人物同一性、所要時間の評価も実施していない。既存2作品は `requires-human-playtest-after-migration`、生成対話型は `architecture-reference-not-wave1` として記録する。機械検証の成功を、人の評価完了へ読み替えない。
